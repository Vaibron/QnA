# QnA API Service

FastAPI-сервис для системы вопросов и ответов (QnA). Позволяет пользователям задавать вопросы, отвечать на них и управлять своим контентом.

## 🚀 Основные возможности

- **Аутентификация и авторизация** (JWT-токены)
- **Управление вопросами** (создание, просмотр, удаление)
- **Управление ответами** (добавление, просмотр, удаление)
- **Подтверждение email** при регистрации (SMTP)
- **Панель администратора** для управления данными
- **Каскадное удаление** вопросов и ответов
- **Валидация данных** через Pydantic-схемы

## 🛠 Технологический стек

- **FastAPI** - веб-фреймворк
- **PostgreSQL** - база данных
- **SQLAlchemy** - ORM с асинхронной поддержкой
- **Alembic** - миграции базы данных
- **Docker & Docker Compose** - контейнеризация
- **JWT** - аутентификация
- **SQLAdmin** - панель администратора
- **Pytest** - тестирование
- **FastAPI-Mail** - отправка email через SMTP

## 📁 Структура проекта

```
QnA/
├── apps/
│   ├── auth/           # Аутентификация и пользователи
│   ├── qna/            # Вопросы и ответы
│   └── admin/          # Панель администратора
├── core/               # Базовая конфигурация и утилиты
├── tests/              # Тесты
├── alembic/            # Миграции базы данных
├── requirements.txt    # Зависимости
├── .env                # Файл конфигурации
├── docker-compose.yml  # Docker Compose
├── Dockerfile          # Docker образ
├── init_superuser.py   # Создание суперпользователя
└── main.py             # Точка входа
```

## 🚀 Быстрый запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/Vaibron/QnA.git
cd QnA
```

### 2. Настройка конфигурации (опционально)

#### Обязательные настройки (.env файл)

```env
# Настройки базы данных
DB_USER=qna_admin
DB_PASSWORD=admin
DB_HOST=db
DB_PORT=5432
DB_NAME=qna_db

# Секретный ключ приложения
SECRET_KEY=a78435bc1f428b2b10180deebf7865d7

# Базовый URL приложения
BASE_URL=http://127.0.0.1:8000
```

#### Настройки SMTP для подтверждения email (сейчас отключено SMTP_ENABLED=No)

```env
# Включение/отключение SMTP
SMTP_ENABLED=Yes

# SMTP-сервер (пример для Яндекс)
SMTP_HOST=smtp.yandex.ru

# Порт SMTP (465 - SSL, 587 - STARTTLS)
SMTP_PORT=465

# Логин для SMTP (обычно email)
SMTP_USER=your-email@yandex.ru

# Пароль или app-password для SMTP
SMTP_PASSWORD=your-app-password

# От кого отправляются письма
SMTP_FROM=your-email@yandex.ru
```

### 3. Запуск проекта с Docker Compose

```bash
sudo docker compose up -d --build
```

Приложение будет доступно по адресу: http://localhost:8000

- **API Documentation**: http://localhost:8000/docs
- **Admin Panel**: http://localhost:8000/admin

### 4. Создание суперпользователя

```bash
sudo docker exec -it qna-app /bin/bash
python init_superuser.py
```

Следуйте инструкциям для создания учетной записи администратора.

### 5. Остановка контейнеров проекта

```bash
# Остановка контейнеров qna-app и qna-db
sudo docker compose stop qna-app qna-db
```

## 📚 API Endpoints

### Аутентификация (`/auth`)

- `POST /auth/register` - Регистрация нового пользователя (отправляет email для подтверждения)
- `POST /auth/login` - Вход в систему
- `POST /auth/refresh` - Обновление токенов
- `GET /auth/verify` - Подтверждение email по ссылке из письма
- `PUT /auth/update` - Обновление профиля
- `PUT /auth/change-password` - Смена пароля
- `DELETE /auth/delete` - Удаление аккаунта

### Вопросы (`/questions`)

- `GET /questions/` - Получить список всех вопросов (публичный)
- `POST /questions/` - Создать новый вопрос (требует аутентификации)
- `GET /questions/{id}` - Получить вопрос с ответами (публичный)
- `DELETE /questions/{id}` - Удалить вопрос (только автор)

### Ответы (`/answers`)

- `POST /questions/{id}/answers/` - Добавить ответ к вопросу (требует аутентификации)
- `GET /answers/{id}` - Получить информацию об ответе (публичный)
- `DELETE /answers/{id}` - Удалить ответ (только автор)

## 👨‍💼 Панель администратора

Доступна по адресу: http://localhost:8000/admin

Для доступа требуется учетная запись суперпользователя (создается через `init_superuser.py`). В панели доступно управление:

- Пользователями
- Вопросами
- Ответами

## ✉️ Функционал отправки email

### При регистрации пользователя:
1. Пользователь регистрируется через `/auth/register`
2. Система генерирует токен подтверждения
3. Отправляется email с ссылкой для подтверждения
4. Пользователь переходит по ссылке для активации аккаунта

### Содержание письма подтверждения:
```
Здравствуйте, {username}!

Спасибо за регистрацию в QnA. Пожалуйста, подтвердите ваш email, перейдя по ссылке:
{base_url}/auth/verify?token={verification_token}

Если вы не регистрировались, просто проигнорируйте это письмо.
```

### Без SMTP:
Если `SMTP_ENABLED=No`, регистрация будет работать, но подтверждать необходимо вручную через БД или Админку:


## 🔄 Миграции базы данных

Миграции выполняются автоматически при запуске контейнера. Для ручного управления:

```bash
# Создание новой миграции
sudo docker exec -it qna-app alembic revision --autogenerate -m "Описание изменений"

# Применение миграций
sudo docker exec -it qna-app alembic upgrade head

# Откат миграции
sudo docker exec -it qna-app alembic downgrade -1
```

## 📊 Модели данных

### User (Пользователь)
- `id` (String/UUID) - уникальный идентификатор
- `username` (String) - имя пользователя
- `email` (String) - электронная почта (уникальный)
- `hashed_password` (String) - хеш пароля
- `is_verified` (Boolean) - подтвержден ли email
- `is_superuser` (Boolean) - является ли администратором
- `created_at` (DateTime) - дата регистрации

### Question (Вопрос)
- `id` (Integer) - первичный ключ
- `text` (String) - текст вопроса (уникальный)
- `author_id` (String) - ID автора (ссылка на User, каскадное удаление)
- `created_at` (DateTime) - дата создания

### Answer (Ответ)
- `id` (Integer) - первичный ключ
- `question_id` (Integer) - ID вопроса (каскадное удаление)
- `user_id` (String) - ID пользователя (каскадное удаление)
- `text` (String) - текст ответа
- `created_at` (DateTime) - дата создания

## 🔒 Безопасность

- **Хеширование паролей**: bcrypt
- **Аутентификация**: JWT-токены с ограниченным временем жизни
- **Валидация данных**: Pydantic схемы для всех входных данных
- **Защита от SQL-инъекций**: параметризованные запросы через SQLAlchemy
- **Каскадное удаление**: автоматическое удаление связанных данных
- **Подтверждение email**: обязательное для критических операций

## ⚠️ Бизнес-логика

- Нельзя создать ответ к несуществующему вопросу
- Один пользователь может оставлять multiple ответов на один вопрос
- При удалении вопроса автоматически удаляются все его ответы
- Только автор может удалять свои вопросы и ответы
- **Email должен быть подтвержден** для полноценного использования API
- При смене email статус подтверждения сбрасывается

## 📝 Логирование

Приложение использует структурированное логирование с уровнями:
- `DEBUG` - детальная отладка операций БД и токенов
- `INFO` - основные события (регистрация, вход, создание контента)
- `WARNING` - потенциальные проблемы (повторные email, неверные пароли)
- `ERROR` - критические ошибки (ошибки SMTP, проблемы с БД)

## 🔧 Разработка

### Локальная разработка без Docker

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск PostgreSQL локально
# Настройте DATABASE_URL в .env

# Запуск приложения
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

## 📋 Полезные команды Docker

### Проверка статуса контейнеров
```bash
sudo docker compose ps
```

### Просмотр логов конкретного сервиса
```bash
sudo docker compose logs qna-app -f
sudo docker compose logs qna-db -f
```

### Остановка и запуск сервисов
```bash
# Остановка всех сервисов
sudo docker compose stop

# Запуск остановленных сервисов
sudo docker compose start

# Перезагрузка конкретного сервиса
sudo docker compose restart qna-app
```
